# matrix
This library is a compilation of tools to use matrix and vectors

## Use
You can ```make``` the library and then use it with the different headers in the ``include`` directory
``matrix.hpp`` is the general header that you may want to include if you want the entierety of the library. Otherwise, you can include only the part wanted.
